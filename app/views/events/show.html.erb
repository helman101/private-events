<div class="container mt-5">
  <h1><%= @event.name %></h1>
  <div class="container p-3">
    <p><%= @event.description %></p>
    <p><%= @event.date.strftime('Event date: %m/%d/%Y') %></p>
    <span><%= @event.creator.name %></span>
  </div>
  <div class="container mt-5">
    <h3>Attendance</h3>
    <ul>
      <%= attendee_name(@event) %>
    </ul>
  </div>

  <%= invite_or_attend(@event) %>
</div>