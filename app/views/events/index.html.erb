<h1>Events</h1>
<% @events.each do |event| %>
  <div>
    <h3><%= event.name %></h3>
    <p><%= event.description %></p>
    <span><%= event.date.strftime("%-d-%-m-%y %H:%M") %></span>
  </div>
<% end %>

